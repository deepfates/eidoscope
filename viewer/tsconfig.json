{
  "compilerOptions": {
    "target": "esnext",
    "module": "esnext",
    "moduleResolution": "bundler",
    "allowImportingTsExtensions": true,
    "noEmit": true,
    "strict": true,
    "skipLibCheck": true,
    "lib": ["esnext", "dom", "dom.iterable"],
    "types": []
  },
  "//": "Type-checks the viewer that vite build (esbuild, no typecheck) and the root tsconfig (include=[src,test]) both skip. tsc uses this include for the plain .ts logic; svelte-check reuses the same tsconfig but discovers .svelte files itself.",
  "include": ["src/**/*.ts"]
}
