{
  "name": "eidoscope-viewer",
  "private": true,
  "type": "module",
  "description": "The eidoscope viewer — Svelte + deck.gl app that consumes a MapContract and builds one self-contained HTML.",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "@sveltejs/vite-plugin-svelte": "^4.0.0",
    "@tailwindcss/vite": "^4.0.0",
    "svelte": "^5.0.0",
    "tailwindcss": "^4.0.0",
    "vite": "^6.0.0",
    "vite-plugin-singlefile": "^2.0.0"
  },
  "dependencies": {
    "apache-arrow": "^18.0.0",
    "deck.gl": "^9.0.0"
  }
}
